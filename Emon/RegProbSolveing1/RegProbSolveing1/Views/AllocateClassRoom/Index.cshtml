@model IEnumerable<RegProbSolveing1.Models.AllocateClassRoom>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Days.DaysName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rooms.RoomsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Days.DaysName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rooms.RoomsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllocateClassRoomId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllocateClassRoomId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllocateClassRoomId })
        </td>
    </tr>
}

</table>
