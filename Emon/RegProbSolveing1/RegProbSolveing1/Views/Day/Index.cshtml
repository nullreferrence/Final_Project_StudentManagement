@model IEnumerable<RegProbSolveing1.Models.Days>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DaysName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DaysName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DaysId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DaysId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DaysId })
        </td>
    </tr>
}

</table>
