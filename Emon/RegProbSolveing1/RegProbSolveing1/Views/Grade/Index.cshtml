@model IEnumerable<RegProbSolveing1.Models.GradeLatter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GradeLatterName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GradeLatterName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GradeLatterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GradeLatterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GradeLatterId })
        </td>
    </tr>
}

</table>
